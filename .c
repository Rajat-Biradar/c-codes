#include<stdio.h>
#include<math.h>
int main()
    { int a,b,sum;
    
    printf("enter the numb a,b:");
    scanf("%d%d",a,&b);
    sum=a+b;
    printf("\na=%d,&b=%d ,& sum=%d",sum);
    return(0);
    }
    
    
